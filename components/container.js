//TODO
export default function Container({ children }) {
  return (
    <div
      style={{
        margin: "3rem",
        margin: "calc(7vw + 2rem)",
      }}
    >
      {children}
    </div>
  );
}
